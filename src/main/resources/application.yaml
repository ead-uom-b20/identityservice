server:
  port:
    8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lambda_users?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password:
    driverClassName: com.mysql.cj.jdbc.Driver
  application:
    name: identity-service
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update